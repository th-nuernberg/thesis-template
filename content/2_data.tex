%----------------------------------------------------------------------
\chapter{Data}
\label{ch:data}
%----------------------------------------------------------------------

\Blindtext

Data are based in the \footnote{\url{\therepository}} repository. The data is stored in the \texttt{data} directory, and the code for processing the data is stored in the \texttt{code} directory.

Table~\ref{tab:example} shows an example dataset that we will use for our experiments.

\begin{table}[ht]
    \centering
    \begin{tabular}{l c r}
        \hline
        \textbf{Name} & \textbf{Value} & \textbf{Unit} \\
        \hline
        Temperature & 23.5 & °C \\
        Pressure & 1013 & hPa \\
        Humidity & 45 & \% \\
        \hline
    \end{tabular}
    \caption{\ifde Beispiel-Datensatz\else Example Dataset\fi}
    \label{tab:example}
\end{table}
